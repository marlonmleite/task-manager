import React from 'react'
import { Loading, LoadingContent as LoadingContentStyled } from './styled'

const LoadingContent = () => (
  <LoadingContentStyled>
    <Loading />
  </LoadingContentStyled>
)

export default LoadingContent
